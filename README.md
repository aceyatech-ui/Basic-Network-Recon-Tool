# Basic Recon Tool

A simple Python port scanner for network reconnaissance and learning cybersecurity fundamentals.

## Features

- Scans common ports on a target host
- Reports open ports
- Lightweight and easy to modify

## Requirements

- Python 3.6+
- No external libraries (uses built-in `socket`)

## Usage

```bash
python port_scanner.py
